/// <reference types="cypress" />

describe('Contact form', () => {
  beforeEach(() => {
    cy.visit('/')
  })

  afterEach(() => {})

  it('Contact us through form', () => {
    // dosta promjena
  })
})
